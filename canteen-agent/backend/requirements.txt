# Web 框架
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
# 数据库
sqlalchemy>=2.0
pymysql>=1.1
aiomysql>=0.2.0
cryptography>=41.0      # pymysql 连接 MySQL 8 的 caching_sha2_password 需要
alembic>=1.13
# 配置 / 校验
pydantic>=2.7
pydantic-settings>=2.0
# 定时任务 / 队列 / 外部调用
apscheduler>=3.10
redis>=5.0
httpx>=0.27

# 说明：LLM / 向量库相关依赖只在 agent 微服务里使用，
# backend 通过 HTTP 调 agent（AGENT_INTERNAL_URL），不再直接依赖 langchain / pymilvus。
